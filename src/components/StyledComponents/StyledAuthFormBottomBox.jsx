import { Box, styled } from "@mui/material";

const StyledAuthFormBottomBox = styled(Box)(({ theme }) => ({
  padding: "1rem",
  backgroundColor: theme.palette.lightGray.dark,
  borderRadius: "0 0 1rem 1rem",
}));

export { StyledAuthFormBottomBox };
